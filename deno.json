{
  "name": "@zinc/core",
  "version": "0.1.0",
  "exports": "./src/index.ts",
  "tasks": {
    "quickstart:server": "deno run --allow-ffi --allow-env examples/quickstart/server.ts",
    "quickstart:client": "deno run --allow-ffi --allow-env examples/quickstart/client.ts",
    "demo:server": "deno run --allow-ffi --allow-env examples/bun_server.ts",
    "demo:client": "deno run --allow-ffi --allow-env examples/deno_client.ts",
    "test": "deno test --allow-ffi --allow-env tests/",
    "bench": "deno bench --allow-ffi --allow-env"
  },
  "fmt": { "lineWidth": 100 },
  "lint": { "rules": { "tags": ["recommended"] } }
}
